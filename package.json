{
  "name": "tasks-note-app",
  "version": "1.0.0",
  "description": "Connects to the mongoDB database to perform CRUD operations on the collection tasks",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index"
  },
  "author": "Dhanush",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.2",
    "mongoose": "^6.0.9"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
